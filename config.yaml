# Misskeybot りいなちゃん Phase 3 完全版設定ファイル
# データベース + ログメンテナンス機能

# キャラクタープロンプトファイル
character_prompt_file: "katariina_prompt.md"

# 投稿設定
posting:
  default_visibility: "home"  # home / public / followers / specified
  
  night_mode:
    enabled: true
    start_hour: 23
    end_hour: 5
  
  random_post:
    enabled: true
    interval_minutes: 60
  
  scheduled_posts:
    enabled: true
    posts:
      "07:30":
        - "おはよー!今日もいい天気だね☀️"
        - "朝だよー!みんな起きてる?"
        - "Huomenta! ☕"
      "14:30":
        - "午後のおやつタイムだね🍰"
        - "そろそろ疲れてきた?休憩しよ!"
        - "Iltapäivän kahvitauko! ☕"
      "19:30":
        - "今日もお疲れ様!晩ごはん何食べる?"
        - "夕方だね〜そろそろ帰る時間?"
        - "Hyvää iltaa! 🌙"
      "22:30"
        - "お風呂入った？"
        - "おーい　もうそろそろ寝る時間だよ～"
        - "お布団の準備はできてる？　わたしはできてる"

# フォロー管理
follow:
  auto_follow_back: false  # 自動フォローバック無効 (キーワードのみ)
  auto_unfollow_back: true
  check_interval_minutes: 30  # WebSocket補完用
  
  keyword_follow_back:
    enabled: true
    keywords:
      - "フォローして"
      - "followして"
      - "フォローお願い"

 # ★ タイムライン連動投稿 (新規追加)
  timeline_post:
    enabled: true                  # 有効/無効
    source: "global"                 # home/local/global (どのタイムラインを読むか)
    interval_minutes: 30           # 実行間隔 (分)
    max_notes_fetch: 20            # 取得するノート数
    min_keyword_length: 2          # キーワードの最小文字数
    ng_words:                      # NGワード (除外する単語)
      - "死ね"
      - "バカ"
      - "殺"
      - "うざい"
      # 必要に応じて追加
      
# リプライ設定
reply:
  enabled: true
  mutual_only: true  # 相互フォローのみ返信
  rate_limit:
    max_per_user_per_hour: 3

# メンテナンス設定 (Phase 3 新機能)
maintenance:
  enabled: true
  
  # データベース: 古いレコード削除
  cleanup_time: "03:00"  # 毎日実行時刻
  cleanup_days: 30       # 何日以前のレコードを削除するか
  
  # データベース: バックアップ
  backup_time: "04:00"   # 毎日実行時刻
  backup_compress: true  # gzip圧縮するか
  keep_backups: 7        # 保持するバックアップ数
  
  # ログ: ローテーション + 古いログ削除
  log_rotate_time: "05:00"  # 毎日実行時刻
  log_cleanup_days: 30      # 何日以前のログを削除するか
  
  # 統計情報ログ出力
  stats_time: "06:00"    # 毎日実行時刻

# システム設定
settings:
  timezone: "Asia/Tokyo"
  database_path: "data/riina_bot.db"
